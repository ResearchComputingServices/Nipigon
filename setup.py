from setuptools import setup

setup(
    name="ExDocGen",
    version="0.0.1",
    description="Performs intelligent text extraction from highly structured PDF documents.",
    install_requires=[],
    packages=["ExDocGen"],
)